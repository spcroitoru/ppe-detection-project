# --- Core ML ---
# NOTE: torch and torchvision are installed SEPARATELY (see README / Step 1 instructions),
# depending on CPU vs GPU, to get the correct CUDA build.
ultralytics>=8.3.40
# --- Experiment tracking ---
mlflow>=2.18.0
# --- EDA / Visualization ---
# NOTE: no strict pinned versions - Python 3.14 is new, and old pinned versions don't have
# precompiled wheels for 3.14 (would force local compilation, requiring Visual Studio Build Tools).
# Letting pip pick the newest compatible version, which already has a wheel for 3.14.
jupyter>=1.1.1
notebook>=7.3.1
matplotlib>=3.10.0
seaborn>=0.13.2
pandas>=2.2.3
numpy>=2.1.0
opencv-python>=4.10.0
Pillow>=11.0.0
# --- Testing ---
pytest>=8.3.4
pytest-cov>=6.0.0
# --- Utilities ---
pyyaml>=6.0.2
tqdm>=4.67.1